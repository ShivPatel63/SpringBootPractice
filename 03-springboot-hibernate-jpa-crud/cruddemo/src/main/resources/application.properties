# DB Connection
spring.datasource.url=jdbc:mysql://localhost:3309/student_tracker
spring.datasource.username= springstudent
spring.datasource.password=springstudent

# Turn off Spring boot banner
spring.main.banner-mode=off

# Reduce logging level and set to just warnings
logging.level.root=warn

# Adding logging configs to display SQL Statements
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.orm.jdbc.bind=trace

# Config JPA/Hibernate to auto create the tables
# use update if we want to keep our old data.
spring.jpa.hibernate.ddl-auto=create
